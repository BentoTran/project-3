services:
  db:
    container_name: workout_planner_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: IamG0d
      POSTGRES_DB: workout_planner
    ports:
      - "5432:5432"